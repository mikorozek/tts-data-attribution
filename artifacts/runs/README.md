# Run artifacts

Ignored local/cluster outputs belong here. Each run must preserve the resolved config, source Git revision, upstream revisions, random seeds, split and mask hashes, environment manifest, metrics, and checkpoint metadata.
