# Local model snapshots

This directory is ignored except for this README. Model-specific download instructions and exact revisions belong to the experiment that uses them.

Do not commit model weights. Record repository IDs, immutable revisions, file hashes, and expected local paths in an experiment-owned source manifest.
